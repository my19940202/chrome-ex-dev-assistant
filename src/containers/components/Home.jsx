// 默认界面
import React, {useState, useCallback} from 'react';

export const Home = () => {
    const [collapsed, setCollapsed] = useState(false);
    return (
        <div>
            主页
        </div>
    );
};
